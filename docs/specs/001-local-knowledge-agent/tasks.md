# 实施任务：本地知识整理 Agent

| 字段     | 内容                                       |
| -------- | ------------------------------------------ |
| 规范编号 | `LKA-001`                                  |
| 状态     | 已完成，Phase 0 至 Phase 8 验收通过        |
| 执行原则 | Phase 1 特征测试通过前，不删除生产功能代码 |

## 任务标记

- `[ ]` 未开始，`[~]` 进行中，`[x]` 已完成，`[!]` 阻塞。
- 每项任务必须关联功能需求、非功能需求或项目原则。
- 只有验证命令和人工验收同时通过，阶段才能标记完成。
- 本文件当前仅描述计划；规范确认后才授权开始生产代码改造。

## Phase 0：确认范围和建立基线

- [x] **T0.1 确认目标和非目标**：关联项目原则 1、9 和需求规范第 2、3、10 节。聊天、会话、用户画像、人格 Prompt 和聊天型 MCP/Skills 已确认为目标产品范围外能力。
- [x] **T0.2 建立干净的 Git 基线**：关联迁移安全。已核实并恢复 stash 中的 lockfile 一致性修改，全部门禁通过，并建立本地基线提交和标签。
- [x] **T0.3 修复或调整现有浏览器测试**：关联 NFR-006 和成功指标。移动端 hydration 竞争已修复，图谱 E2E 已匹配折叠筛选器的真实交互，Playwright 可正常退出。
- [x] **T0.4 保存仓库指标**：关联作品集可解释性。代码量、路由数、依赖数、测试数、检索指标和环境信息已记录在 `phase-0-baseline.md`。

### Phase 0 验证

```text
npm run format:check
npm run typecheck
npm run lint
npm run test
npm run eval
npm run build
npm run test:e2e
```

## Phase 1：固定需要保留的行为

- [x] **T1.1 监听到队列的特征测试**：关联 FR-001 至 FR-004、NFR-003。真实 SQLite 已覆盖新增、并发修改、重扫、未变化输入、更新事件和稳定 ID。
- [x] **T1.2 队列到发布的特征测试**：关联 FR-008 至 FR-011。真实 SQLite 和临时文件系统已覆盖 accept、review、reject、冲突、重试和进程恢复。
- [x] **T1.3 固定检索行为**：关联 FR-016 至 FR-019。已固定关键词降级、真实向量、多路查询、文件图谱和 MMR 行为。
- [x] **T1.4 固定索引重建行为**：关联 FR-021。已证明删除 HNSW sidecar 后可从 SQLite 重建，且规范 Markdown 逐字节不变。

## Phase 2：建立显式知识整理 Agent

- [x] **T2.1 定义来源版本契约**：关联 FR-004、FR-023。已增加 `SourceRevisionEvent` TypeScript 类型、Zod schema、规范路径、稳定来源 ID、版本哈希和稳定事件 ID 工厂。
- [x] **T2.2 建立来源注册表**：关联 FR-001、FR-004。`SourceRegistry` 通过 `source_documents` 持久化规范路径、最新成功版本、观察时间、处理时间和健康状态，并同步现有工具来源配置。
- [x] **T2.3 统一 Watcher 输出**：关联 FR-002、FR-003。文件、工具目录和 listen 输入统一构造来源版本事件；add/change/delete/rescan 均通过 `KnowledgeAgent.ingestSourceRevision()` 进入处理链。
- [x] **T2.4 增加 Agent 状态迁移保护**：关联项目原则 1、FR-011、NFR-002。Agent 阶段和兼容队列状态均由集中迁移表校验；processing 中断恢复保留事件 ID、来源 ID 和版本并递增 attempt。
- [x] **T2.5 提取 `KnowledgeAgent` 编排边界**：关联项目原则 1、NFR-007。Watcher、listen、ingest、审核、冲突、重建、replay 和 AuditWorker 已改用 `KnowledgeAgent`；现有 `Orchestrator` 作为内部兼容执行引擎保留。
- [x] **T2.6 输出阶段进度**：关联 FR-023。`processing_attempts` 持久化类型化 `AgentProgressEvent`，结构化日志包含来源、版本、事件、阶段、attempt、耗时、结果、错误和降级能力；`GET /api/listen` 暴露只读状态快照。

## Phase 3：增强去噪和来源追踪

- [x] **T3.1 定义去噪报告契约**：关联 FR-005。每次归一化记录被删除内容的类别和数量。
- [x] **T3.2 调用模型前进行密钥脱敏**：关联项目原则 2 和安全设计。覆盖常见 API Key、Token、Authorization Header 和私钥模式，并增加测试。
- [x] **T3.3 改进语义分块**：关联 FR-006。优先使用 Markdown 章节和会话轮次，长度切分只作为兜底。
- [x] **T3.4 实现来源级和块级身份**：关联 FR-004、FR-007、FR-014。保存来源版本和块哈希，使未变化重扫成为无操作。
- [x] **T3.5 持久化来源关系**：关联 FR-012、FR-015。建立来源版本和知识单元关系，并尽可能迁移现有 memory 来源元数据。

## Phase 4：将保留代码与待删除功能解耦

- [x] **T4.1 从排序中删除画像亲和度**：关联项目原则 8 和非目标。使用明确相关度、质量、时间和访问行为替代画像权重，并更新 Ranker 测试与评测基线。
- [x] **T4.2 将 path resolver 与 PromptCache 解耦**：关联 NFR-007。删除存储迁移中面向 Prompt 和画像的缓存失效依赖。
- [x] **T4.3 将知识加工 AI 契约与聊天事件分离**：关联项目原则 7。保留结构化生成和 Embedding，禁止它们依赖聊天事件类型。
- [x] **T4.4 删除 nightly 画像和聊天路由优化**：关联非目标。后台任务只保留具有现行知识加工需求的部分。
- [x] **T4.5 精简配置持久化**：关联范围控制。通过非破坏性迁移移除未使用的 MCP、Skills、画像和聊天配置；旧数据可以保留，但生产代码不得继续依赖。

## Phase 5：删除范围外功能

- [x] **T5.1 删除聊天页面和导航**：关联 AC-007。聊天页面及组件已删除；首页和导航改为来源、审核和检索入口。
- [x] **T5.2 删除聊天 API 和 Dispatcher**：关联 AC-007。已删除 `/api/chat`、流式/会话路由、`AgentDispatcher`、`ChatHandler`、`AiEvent` 流适配器及聊天专属工具运行时。
- [x] **T5.3 删除聊天会话持久化代码**：关联非目标。运行时代码和测试已删除；未扫描或删除用户已有 JSONL 文件。
- [x] **T5.4 删除用户画像**：关联非目标。updater、API、页面、Prompt 注入、存储辅助函数和画像测试已删除；遗留 `profile.md` 保持原位且继续被 watcher 忽略。
- [x] **T5.5 删除人格 Prompt 管理**：关联非目标。Prompt 页面、API、模板管理器和缓存已删除；知识加工 Prompt 保留在对应服务代码中。
- [x] **T5.6 删除聊天型 MCP 和 Skills**：关联 ADR-005。页面、API、运行时注册、配置兼容服务、scheduler 和 npm 依赖已删除；历史来源枚举保留用于读取旧数据。
- [x] **T5.7 删除浏览器历史采集**：关联范围决策 2。collector、scheduler、启动注册和环境配置已删除。
- [x] **T5.8 更新 API 契约表**：关联 AC-007。旧路由登记已删除，现有来源状态契约已登记；主题学习资料契约随 Phase 6 T6.1 的 schema 和真实路由一起登记，避免预登记 stale route。

## Phase 6：生成可阅读学习资料

- [x] **T6.1 定义主题资料 schema**：关联 FR-013、FR-015。已定义大纲章节、引用知识 ID、来源版本集合、内容哈希和生成时间，并登记真实主题 API。
- [x] **T6.2 实现确定性资料生成器**：关联 FR-013、FR-014。按共享标签和图谱关系稳定分组，模型降级时仍可生成。
- [x] **T6.3 增加可选模型辅助组织**：关联项目原则 3、FR-013。标准模型只能调整既有章节标题和顺序，非法或缺失引用回退确定性结果。
- [x] **T6.4 实现原子发布**：关联 NFR-002。临时文件经完整 schema 与哈希复验后原子替换，失败保留上一合法版本。
- [x] **T6.5 增量刷新受影响主题**：关联 FR-014。发布、删除、重建和冲突解决只刷新实际变化主题，内容哈希不变时跳过写入。

## Phase 7：重构用户界面

- [x] **T7.1 建立处理状态页**：关联 FR-002、FR-023。展示来源健康、最近事件、阶段和结果、降级状态以及待审核数量。
- [x] **T7.2 整合来源设置**：关联 FR-001。支持新增、编辑、停用、移除、扫描以及查看最近来源版本。
- [x] **T7.3 调整检索库**：关联 FR-016 至 FR-018。展示命中通道、主题、来源和最终分数，删除画像相关界面。
- [x] **T7.4 增加主题资料阅读页**：关联 FR-013、FR-015、AC-005。展示大纲、章节、引用、知识链接和来源信息。
- [x] **T7.5 调整审核界面**：关联 FR-020。同时展示候选、来源摘录、判断原因、重复或冲突，以及接受、编辑、拒绝操作。
- [x] **T7.6 验证响应式和可访问性**：关联 NFR-006。检查桌面和移动端、键盘导航、焦点顺序、标签和横向溢出。

## Phase 8：验证和作品集交付

- [x] **T8.1 更新单元和集成测试**：关联项目原则 10。删除范围外测试，增加状态机、来源、主题资料和降级测试。
- [x] **T8.2 重写 Playwright 主流程**：关联项目原则 10。覆盖配置来源、采集、审核、搜索、阅读主题资料、检查来源和移动端布局。
- [x] **T8.3 扩充检索评测**：关联 FR-019。增加含噪、同义词、主题过滤和近似重复场景，并设置明确回归阈值。
- [x] **T8.4 审计依赖和死代码**：关联项目原则 9。通过 import 分析和仓库搜索删除不可达聊天、画像、MCP/Skills 代码及无用依赖。
- [x] **T8.5 更新项目文档**：关联治理规则。重写 README 和 `Agents.md`，更新架构图及偏差表，说明本地运行、隐私边界、故障行为和重建步骤。
- [x] **T8.6 准备面试演示**：关联成功指标。准备确定性演示数据和五分钟讲解脚本，完整展示观察、决策、执行、评价、恢复或审核、发布、检索和来源追踪。
- [x] **T8.7 记录改造前后指标**：关联作品集可解释性。对比代码量、路由数、依赖数、测试覆盖率、检索指标、重复率和处理耗时。

### 最终验证

```text
npm run format:check
npm run typecheck
npm run lint
npm run audit:dead-code
npm run test:coverage
npm run eval
npm run build
npm run test:e2e
```

## 任务依赖关系

```text
Phase 0 -> Phase 1 -> Phase 2 -> Phase 3
                         |
                         v
                     Phase 4 -> Phase 5
                         |         |
                         +----+----+
                              v
                          Phase 6 -> Phase 7 -> Phase 8
```

在 Phase 3 的来源契约和 Phase 4 的解耦稳定后，Phase 6 可以提前并行开始。Phase 1 测试没有通过之前，禁止开始 Phase 5 的生产代码删除。
